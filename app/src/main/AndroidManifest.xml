<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthApp"
        tools:targetApi="31">


        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainScreen">
        </activity>
        <activity android:name=".InputData" />
        <activity android:name="._MainActivity_" />
        <activity android:name="._Login_"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".TempTest_12"/>
        <activity android:name=".FoodInputActivity"/>
        <activity android:name=".FoodMainActivity"/>
        <activity android:name=".DietRecommendationActivity"/>
        <activity android:name=".ExerciseInputData"/>

    </application>

</manifest>
